import 'package:flutter/material.dart';


class SportScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child :Text('SportScreen', style: TextStyle(fontSize: 35, fontWeight: FontWeight.bold),),
    );
  }
}
